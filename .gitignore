# dependencies
node_modules

# build output (Render builds this on deploy)
dist

# misc
.DS_Store
*.log
.env
.env.*
.vite

# preview screenshots & legacy static backup (not part of the app)
preview-*.png
legacy/
